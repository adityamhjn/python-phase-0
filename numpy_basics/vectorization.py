import numpy as np

arr = np.array([1, 2, 3, 4, 5])

# Vectorized operations
#Vectorisation allows operations on entire arrays without explicit loops
print(arr + 10)
print(arr * 2)
print(arr ** 2)
print(np.sin(arr))
print(np.log(arr))