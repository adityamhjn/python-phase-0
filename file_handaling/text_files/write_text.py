with open("output.txt", "w") as file:
    file.write("Machine Learning starts with clean data.")
# This code opens (or creates) a text file named "output.txt" in write mode,
# writes the specified string into it, and then closes the file.